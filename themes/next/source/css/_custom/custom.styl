// ---------------------------------------------------
// 纯黑正文 (#000000) 和 深灰导航栏 (#555555) 分离
// ---------------------------------------------------

// 定义颜色
$pure-black = #000000;
$dark-gray-nav = #555555; // 导航栏使用柔和的深灰色

// 1. 针对主体文本、文章内容和通用段落
body {
  // 注意：我们只设置 body，链接和标题的颜色会在下面覆盖
  color: $pure-black !important; 
}

// 2. 针对文章内所有标题（H1 - H6）
h1, h2, h3, h4, h5, h6 {
  color: $pure-black !important;
}

// 3. 针对文章中的超链接 (Link) 颜色
a {
  color: $pure-black !important;
}

// 4. 针对文章内容中的具体文本块（防止覆盖失败）
.post-body, .post-body p, .post-body li {
  color: $pure-black !important;
  font-weight: 500 !important;
}

// ---------------------------------------------------
// 5. 导航栏（Home, Archives）字体颜色 - 不用纯黑
// ---------------------------------------------------
.site-nav {
  a {
    // 导航栏使用柔和的深灰色，而不是纯黑
    color: $dark-gray-nav !important; 
  }
}