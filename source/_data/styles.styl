// ---------------------------------------------------
// 柔和深灰色字体样式 (#333333)
// ---------------------------------------------------

// 定义新的深灰色
$dark-gray = #333333;

// 针对主体文本、文章内容和通用段落
body {
  color: $dark-gray; 
}

// 针对文章内所有标题（H1 - H6）
h1, h2, h3, h4, h5, h6 {
  color: $dark-gray;
}

// 针对超链接 (Link) 颜色
a {
  color: $dark-gray;
}

// 针对导航栏（书签栏）链接文字
.site-nav {
  a {
    color: $dark-gray;
  }
}

// 针对文章内容中的具体文本块，确保高优先级
.post-body, .post-body p, .post-body li {
  color: $dark-gray;
  font-weight: 500;
}